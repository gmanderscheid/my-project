name: Lint

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  flake8:
    runs-on: ubuntu-latest
    steps:
      - name: Install flake8
        run: pip install flake8
      - name: Run flake8
        run: flake8 .
